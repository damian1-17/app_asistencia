# Desarrollo local — el proxy de Vite redirige /api → Railway
# VITE_API_URL=/api/v1  ← usa proxy (default si no se define)

# Para apuntar directamente a Railway sin proxy, descomenta:
VITE_API_URL=https://appregistro-production.up.railway.app/api/v1
